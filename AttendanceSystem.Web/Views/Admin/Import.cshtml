@{
    ViewData["Title"] = "Import Biometric Data";
}

<div class="page-header">
    <div class="row align-items-center">
        <div class="col-auto">
            <h5 class="page-title mb-0">Biometric Data Import</h5>
        </div>
        <div class="col-auto ms-auto">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-0">
                    <li class="breadcrumb-item"><a asp-controller="Admin" asp-action="Index">Dashboard</a></li>
                    <li class="breadcrumb-item active">Biometric Import</li>
                </ol>
            </nav>
        </div>
    </div>
</div>

<div class="row g-3">
    <!-- ── Left Column: Upload Controls ── -->
    <div class="col-md-6">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center text-white" style="background:#01a9ac;">
                <h5 class="card-header-text text-white mb-0">
                    <i class="fa fa-upload me-2"></i>Upload File (CSV / Excel / MS Access)
                </h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label class="form-label fw-semibold">Select Biometric File <span class="text-danger">*</span></label>
                    <input type="file" id="importFile" class="form-control" accept=".csv,.xlsx,.xls,.mdb,.accdb">
                    <div class="form-text small text-muted">
                        Supported formats: <strong>CSV</strong> (.csv, .txt), <strong>Excel</strong> (.xlsx, .xls), and <strong>Microsoft Access Databases</strong> (.mdb, .accdb — ZKTeco / Att_log / CHECKINOUT tables).
                    </div>
                </div>

                <div class="row g-2 mb-3">
                    <div class="col-md-6">
                        <label class="form-label fw-semibold">From Date</label>
                        <input type="date" id="importFrom" class="form-control">
                    </div>
                    <div class="col-md-6">
                        <label class="form-label fw-semibold">To Date</label>
                        <input type="date" id="importTo" class="form-control">
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button class="btn btn-outline-secondary btn-sm px-3" onclick="previewFile()">
                        <i class="fa fa-eye me-1"></i>Preview & Edit Punches
                    </button>
                    <button class="btn text-white btn-sm px-3" style="background:#01a9ac;" onclick="importDirectFile()">
                        <i class="fa fa-download me-1"></i>Import Directly
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- ── Right Column: Status & Summary ── -->
    <div class="col-md-6">
        <div class="card">
            <div class="card-header fw-semibold" style="background:#f8f9fa;">
                <i class="fa fa-info-circle me-1" style="color:#01a9ac;"></i>Import Summary &amp; Status
            </div>
            <div class="card-body">
                <div id="resultPanel" class="text-muted">
                    <p class="mb-0"><i class="fa fa-info-circle me-1"></i>Select a file and click <strong>Preview &amp; Edit</strong> to review and modify punch data before saving.</p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- ── Interactive & Editable Preview Grid ── -->
<div class="card mt-3" id="previewCard" style="display:none">
    <div class="card-header d-flex justify-content-between align-items-center flex-wrap gap-2">
        <div>
            <span class="fw-semibold text-dark"><i class="fa fa-edit me-1" style="color:#01a9ac;"></i>Editable Punch Data Grid</span>
            <span class="badge bg-secondary ms-2" id="previewCount">0 records</span>
            <small class="text-muted ms-2 d-block d-sm-inline">Edit enroll IDs, punch times, or delete invalid rows before importing.</small>
        </div>
        <div class="d-flex gap-2">
            <button class="btn btn-sm btn-outline-secondary" onclick="addManualRow()">
                <i class="fa fa-plus me-1"></i>Add Row
            </button>
            <button class="btn btn-sm text-white px-3" style="background:#01a9ac;" onclick="importEditedPunches()">
                <i class="fa fa-check-circle me-1"></i>Import Selected Punches
            </button>
        </div>
    </div>
    <div class="card-body p-0" style="max-height:480px;overflow-y:auto">
        <table class="table table-sm table-hover align-middle mb-0" id="previewTable">
            <thead class="table-light sticky-top">
                <tr>
                    <th class="ps-3 text-center" style="width:40px;">
                        <input type="checkbox" id="chkSelectAll" class="form-check-input" checked onclick="toggleSelectAll(this.checked)">
                    </th>
                    <th style="width:40px;">#</th>
                    <th style="width:160px;">Enroll ID <span class="text-danger">*</span></th>
                    <th>Matched Employee</th>
                    <th style="width:230px;">Punch Date &amp; Time <span class="text-danger">*</span></th>
                    <th style="width:120px;">Device ID</th>
                    <th class="text-center" style="width:80px;">Action</th>
                </tr>
            </thead>
            <tbody id="previewBody"></tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="~/js/pages/admin/import.js" asp-append-version="true"></script>
}
